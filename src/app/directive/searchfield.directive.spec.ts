import { SearchfieldDirective } from './searchfield.directive';

describe('SearchfieldDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchfieldDirective();
    expect(directive).toBeTruthy();
  });
});
