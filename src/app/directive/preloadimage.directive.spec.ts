import { PreloadimageDirective } from './preloadimage.directive';

describe('PreloadimageDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadimageDirective();
    expect(directive).toBeTruthy();
  });
});
